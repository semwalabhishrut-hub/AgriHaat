import { PageShell, PageSkeleton } from "@/components/site/page-shell";
export default function BuyersPage() { return <PageShell><PageSkeleton eyebrow="For buyers" title="Find produce worth planning around." description="The buyer workspace will bring verified farms, seasonal availability, and straightforward requests into one place." cards={2} /></PageShell>; }
